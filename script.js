const modal=document.getElementById("modal"),content=document.getElementById("modalContent");
function toggleNav(){document.getElementById("nav").classList.toggle("open")}
function openAuth(type){modal.classList.add("show");content.innerHTML=type==="login"?`<h2>Log in</h2><p>Enter your account details.</p><input type="email" placeholder="Email"><input type="password" placeholder="Password"><button class="primary" onclick="closeModal();toast('Login is ready for secure database connection.')">Log in</button>`:`<h2>Create your profile</h2><p>Start your Walk With Walker creator account.</p><input id="name" placeholder="Full name"><input id="username" placeholder="Username"><input type="email" placeholder="Email"><select id="role"><option>Artist</option><option>Writer</option><option>Gamer</option><option>Creator</option></select><textarea id="bio" placeholder="Short bio"></textarea><button class="primary" onclick="saveDemoProfile()">Create account</button>`}
function closeModal(){modal.classList.remove("show")}
function saveDemoProfile(){let n=document.getElementById("name").value.trim(),u=document.getElementById("username").value.trim();if(!n||!u){toast("Please enter your name and username.");return}localStorage.setItem("ww_profile",JSON.stringify({name:n,username:u,role:document.getElementById("role").value,bio:document.getElementById("bio").value}));closeModal();toast("Profile saved on this device.")}
function viewProfile(n,r){modal.classList.add("show");content.innerHTML=`<h2>${n}</h2><p><b>${r}</b></p><p>This creator page is ready for the real database.</p><button class="primary" onclick="closeModal()">Close</button>`}
function searchProfiles(){let q=document.getElementById("search").value.toLowerCase();document.querySelectorAll(".profile").forEach(p=>p.style.display=p.innerText.toLowerCase().includes(q)?"block":"none")}
function toast(m){let t=document.getElementById("toast");t.textContent=m;t.style.display="block";setTimeout(()=>t.style.display="none",2300)}